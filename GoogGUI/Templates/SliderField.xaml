<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="SliderField">
        <Border Height="38" Margin="0,3">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" MaxWidth="300" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource NormalText}" Margin="0,0,12,0">
                    <Run Text="{Binding Value}" />
                </TextBlock>
                <Slider Style="{StaticResource HSlider}" Grid.Column="1" IsSnapToTickEnabled="{Binding TickEnabled}" TickFrequency="{Binding TickFrequency}" Minimum="{Binding Minimum}" Maximum="{Binding Maximum}" Value="{Binding Value, Mode=TwoWay}" />
            </Grid>
        </Border>       
    </DataTemplate>
</ResourceDictionary>