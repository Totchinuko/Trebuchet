<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="ChooseNameModal">
        <Grid VerticalAlignment="Center" Margin="12" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ToFocus" Tag="Submit" Height="25" VerticalContentAlignment="Center" Text="{Binding Name, Mode=TwoWay, Delay=400, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
            <Button Grid.Column="1" Style="{StaticResource ButtonIconGreen}" Content="{Binding ButtonLabel}" Command="{Binding ValidateCommand}" />
        </Grid>
    </DataTemplate>
</ResourceDictionary>