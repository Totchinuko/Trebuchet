<!--<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Controls="clr-namespace:Trebuchet.Controls">
    <DataTemplate x:Key="FloatField">
        <Border Height="38" Margin="0,3" MinWidth="300" MaxWidth="400" HorizontalAlignment="Left">
            <Controls:FloatBox VerticalAlignment="Center"
                             Height="25"
                             MinValue="{Binding Minimum}"
                             MaxValue="{Binding Maximum}"
                             Value="{Binding Value, Mode=TwoWay}" />
        </Border>
    </DataTemplate>
</ResourceDictionary>