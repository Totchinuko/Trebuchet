<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:modals="clr-namespace:Trebuchet.Modals">
    <DataTemplate x:Key="TestliveSelection" x:DataType="modals:TestliveModal">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Classes="Base Blue"
                    Height="128"
                    Width="128"
                    Command="{Binding LiveCommand}">
                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Svg Path="avares://TrebuchetUtils/Assets/SvgIcons/icon.window.svg" Width="32" Height="32"  />
                    <TextBlock FontWeight="Bold" FontSize="16" Grid.Row="2" Text="Live" />
                </Grid>
            </Button>
    
            <Button Grid.Column="3"
                    Classes="Base Blue"
                    Height="128"
                    Width="128"
                    Command="{Binding TestLiveCommand}">
                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Svg Path="avares://TrebuchetUtils/Assets/SvgIcons/icon.window.bug.svg" Width="32" Height="32"  />
                    <TextBlock FontWeight="Bold" FontSize="16" Grid.Row="2" Text="Test Live" />
                </Grid>
            </Button>
        </Grid>
    </DataTemplate>
</ResourceDictionary>