<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Trebuchet">
    <DataTemplate x:Key="TestliveSelection">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Height="128"
                    Width="128"
                    Command="{Binding LiveCommand}"
                    local:GuiExtensions.Icon="pack://application:,,,/Trebuchet;component/Icons/Window.png"
                    Style="{StaticResource ButtonTestlive}">
                <TextBlock FontSize="16" Text="Live" />
            </Button>

            <Button Grid.Column="3"
                    Height="128"
                    Width="128"
                    local:GuiExtensions.Icon="pack://application:,,,/Trebuchet;component/Icons/BugWindow.png"
                    Command="{Binding TestLiveCommand}"
                    Style="{StaticResource ButtonTestlive}">
                <TextBlock FontSize="16" Text="Test Live" />
            </Button>
        </Grid>
    </DataTemplate>
</ResourceDictionary>